---
# Empty string installs the latest stable release reported by dl.k8s.io.
kubectl_version: ""
kubectl_install_path: /usr/local/bin/kubectl
kubectl_arch_map:
  x86_64: amd64
  aarch64: arm64
kubectl_arch: "{{ kubectl_arch_map[ansible_architecture] | default('amd64') }}"
