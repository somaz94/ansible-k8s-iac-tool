---
- name: Validate required role variables
  ansible.builtin.assert:
    that:
      - home_user is defined
      - home_user is string
      - home_user | length > 0
    fail_msg: "install_krew requires `home_user` to be set (non-empty string)."
    quiet: true

- name: Check if krew is installed
  ansible.builtin.shell: kubectl krew version
  args:
    executable: /bin/bash
  environment:
    PATH: "/usr/local/bin:{{ ansible_env.PATH }}"
  register: krew_check
  failed_when: false
  changed_when: false

- name: Ensure git is installed
  ansible.builtin.package:
    name: git
    state: present
  become: true

- name: Get home directory of specific user
  ansible.builtin.getent:
    database: passwd
    key: "{{ home_user }}"
  when: krew_check.rc != 0

- name: Set user home directory fact
  ansible.builtin.set_fact:
    user_home_dir: "{{ ansible_facts.getent_passwd[home_user][4] }}"
  when: krew_check.rc != 0

- name: Install krew
  when: krew_check.rc != 0
  block:
    - name: Download and Install krew
      ansible.builtin.shell: |
        set -eo pipefail
        if kubectl krew version >/dev/null 2>&1; then
          echo "krew already installed"
          exit 0
        fi
        INSTALL_DIR="$(mktemp -d "${HOME}/krew-install.XXXXXX")"
        trap 'rm -rf "${INSTALL_DIR}"' EXIT
        cd "${INSTALL_DIR}"
        OS="$(uname | tr '[:upper:]' '[:lower:]')"
        KREW="krew-${OS}_{{ krew_arch }}"
        curl -fsSLO "https://github.com/kubernetes-sigs/krew/releases/latest/download/${KREW}.tar.gz"
        tar zxvf "${KREW}.tar.gz"
        chmod +x ./"${KREW}"
        ./"${KREW}" install krew
      args:
        executable: /bin/bash
      environment:
        KREW_ROOT: "{{ user_home_dir }}/.krew"
        PATH: "/usr/local/bin:{{ ansible_env.PATH }}:{{ user_home_dir }}/.krew/bin"
      become: false
      register: krew_install_result
      changed_when: "'Added krew' in krew_install_result.stdout"
