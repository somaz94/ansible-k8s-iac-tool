---
# Required: the unprivileged user under which krew is installed.
# Override via inventory; no usable default is provided.
home_user: ""
krew_arch_map:
  x86_64: amd64
  aarch64: arm64
krew_arch: "{{ krew_arch_map[ansible_architecture] | default('amd64') }}"
